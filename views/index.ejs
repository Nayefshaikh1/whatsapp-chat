<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Mine Whatsapp</title>
</head>

<body>

    <h3>All chats!</h3>
    <form method="get" action="/chats/new">
        <button>New Chat</button>
        <br>
    </form>
    <% for(let chat of chats){%>
        <div class="chat">
            <p>From:<%= chat.from %>
            </p>
            <div class="msg">
                <p>Msg:<%= chat.msg %></p>
                <form method="get" action="/chats/<%= chat._id %>/edit">
                    <button>edit</button>
                </form>
                <br>
                <form method="post" action="/chats/<%= chat._id %>?_method=DELETE">
                    <button>Delete</button>
                </form>
                <br
                <form method="get" action="/chats/<%= chat._id %>">
                    <button>Show</button>
                </form>
            </div>
            <p>Recived by:<%= chat.to %></p></i>
            </p>
            <hr>
            <p>
                <%=chat.created_at.toString().split(" ")[4] %></p><br>
            <p><%=chat.created_at.toString().split(" ").slice(0,4).join("-") %>
            </p><br>
        </div>
        <br><br> <% } %>
</body>

</html>